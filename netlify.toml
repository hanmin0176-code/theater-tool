[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev:vite -- --port 5173"
  targetPort = 5173
  port = 8888
  framework = "#custom"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
